# qwik

A Claude Code plugin bundling the `qwik` Agent Skill: expert guidance for the
Qwik framework (v1 and v2) and its meta-framework — Qwik City in v1, Qwik Router
in v2.

## Install

```bash
/plugin marketplace add 46ki75/claude-plugins
/plugin install qwik@46ki75-plugins
```
