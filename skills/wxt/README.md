# wxt

A Claude Code plugin bundling the `wxt` Agent Skill: expert guidance for
building browser extensions with WXT — project setup, file-based entrypoints,
and extension APIs.

## Install

```bash
/plugin marketplace add 46ki75/claude-plugins
/plugin install wxt@46ki75-plugins
```
